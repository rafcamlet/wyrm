#!/usr/bin/env ruby

require_relative '../lib/scratch'

path = ARGV[0]
code = File.read(path)

evaluator = Scratch::Evaluator.new(code)
evaluator.eval

evaluator.render if ENV['DEBUG'] == 'true'


exit(0)
